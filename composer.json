{
    "name": "nauka/deploy_backup",
    "description": "Gets, unpacks and configures backups of sites",
    "type": "project",
    "license": "MIT",
    "authors": [
        {
            "name": "Anton Batashev",
            "email": "aa.batashev@npo-nauka.ru"
        }
    ],
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "require": {
        "php": "^8.3",
        "symfony/yaml": "^7.0",
        "symfony/filesystem": "^7.0",
        "symfony/console": "^7.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0",
        "symfony/var-dumper": "^7.0"
    },
    "suggest": {
        "symfony/dotenv": "Needed for parsing .env",
        "vlucas/phpdotenv": "Needed for parsing .env"
    },
    "config": {
        "platform": {
            "php": "8.3"
        }
    }
}
